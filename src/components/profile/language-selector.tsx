import { View, Text } from "react-native";
import { profileStyles } from "../../screens/profile-screen/profile-styles";

export function LanguageSelector() {
  return (
    <View>
      <Text style={profileStyles.profileDefaultLabel}>LanguageSelector</Text>
    </View>
  );
}
